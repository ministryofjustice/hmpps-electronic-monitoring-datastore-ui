{% macro safeGovukDate(value) %}
  {{ value | mojDate("date") if value else "" }}
{% endmacro %}

{% macro safeGovukTime(value) %}
  {{ value | mojDate("time") if value else "" }}
{% endmacro %}

{% macro safeGovukDateTime(value) %}
  {{ value | mojDate('datetime') if value else "" }}
{% endmacro %}
