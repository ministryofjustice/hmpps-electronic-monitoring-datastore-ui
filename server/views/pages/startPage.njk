{% extends "../partials/layout.njk" %}
{# {% set pageTitle = makePageTitle({ pageHeading: "Start page" }) %} #}

{% set searchUrl = "/search" %}

{% block content %}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds-from-desktop">
            <h1 class="govuk-heading-l">{{ applicationName }}</h1>
        </div>
        <div class="govuk-grid-column-two-thirds">
            <p>
                Use this service for CMT users to search historial electronic monitoring data.
            </p>
            {{ govukButton({
                text: "Go to search page",
                href: searchUrl,
                isStartButton: true
            }) }}
        </div>
    </div>

{% endblock %}